#include <stdio.h>
#include "sdkconfig.h"
#include "driver/gpio.h" 

void init(void)
{
    // Initialize timer 
    // timer_init(GROUP_NUM);
}

void app_main(void)
{
    init();
    while(1)
    {
        
    }
}
